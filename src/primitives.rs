fn main() {
    let mut mutable = 12;
    mutable = 21;
    println!("{}", mutable);
    let mutable = true;
    println!("{}", mutable);
    let mutable = false;
    println!("{}", mutable);
    let mutable = true;
    println!("{}", mutable);
}